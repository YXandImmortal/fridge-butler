spring:
  application:
    name: fridge-butler-backend
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/fridge_butler_db?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: yx011215

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  security:
    user:
      name: user
      password: 123456

server:
  port: 8080
  servlet:
    context-path: /api

jwt:
  secret: secret-key-2026-fridge-butler
  expiration: 86400000